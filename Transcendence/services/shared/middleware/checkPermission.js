/**
 * @deprecated Prefer importing from '../auth.middleware.js' (identity/chat)
 * or 'workspace_service/.../middleware/permissions.js' (workspace).
 * Kept temporarily so nothing breaks if an old import remains.
 */
export { authenticate } from '../auth.middleware.js';
